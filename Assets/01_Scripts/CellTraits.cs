using UnityEngine;

    public struct CellTraits
    {
        public Color color;
        public float size;

        public CellTraits(Color color, float size)
        {
            this.color = color;
            this.size = size;
        }
    }

